/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'pgumortzasqvwfrydhkn.supabase.co',
      },
    ],
  },
}

export default nextConfig
